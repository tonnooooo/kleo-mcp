# Local picture generation on the GPU (worker/kleo_pictures.py): Stable Diffusion 1.5 through diffusers.
# torch comes from the base image (pytorch/pytorch 2.8.0+cu128); transformers / tokenizers / huggingface-hub are
# pinned by worker/keou/requirements.txt (transformers 5.16.1, huggingface-hub 1.30.0) and must stay satisfied.
diffusers==0.40.0
accelerate==1.15.0
safetensors==0.8.0
pillow>=10.4
# The still gate in worker/kleo_video.py (travel_px) reads clips with OpenCV; without it the gate is silently off.
# Found on 13 September when a run's log said nothing and four generated clips could not even be saved.
opencv-python-headless==4.11.0.86
# The Wan pipelines clean prompts with ftfy and import it lazily: without it every clip dies on NameError.
ftfy==6.3.1
